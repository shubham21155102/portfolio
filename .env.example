# Example environment variables for this project
# For the Next.js web app (client + server routes)
# Place actual secrets in .env.local (do NOT commit secrets into git)

# If you want the Next.js API route to call OpenAI directly, set:
# OPENAI_API_KEY=sk-...        # (server-only, do NOT prefix with NEXT_PUBLIC_)
# OPENAI_MODEL=gpt-4o-mini     # optional, default in ai-agent is gpt-4o-mini

# To let the browser/client call a separate ai-agent microservice, set:
# The client will use NEXT_PUBLIC_AI_ENDPOINT if present (for example:
# NEXT_PUBLIC_AI_ENDPOINT=http://localhost:4000/api/ai)
NEXT_PUBLIC_AI_ENDPOINT=/api/chat

# If you run the separate ai-agent service (in /ai-agent), you can start it
# with `cd ai-agent && npm start` and then point NEXT_PUBLIC_AI_ENDPOINT at
# its /api/ai path (example: http://localhost:4000/api/ai)
